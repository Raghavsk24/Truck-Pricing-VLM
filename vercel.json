{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install --prefix web",
  "buildCommand": "npm run build --prefix web && rm -rf .next && cp -R web/.next .next"
}
